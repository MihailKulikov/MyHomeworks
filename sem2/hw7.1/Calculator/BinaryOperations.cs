namespace Calculator
{
    /// <summary>
    /// Represents binary operations in Calculator
    /// </summary>
    public enum BinaryOperations
    {
        Multiply = '*',
        Add = '+',
        Subtract = '-',
        Divide = '/'
    }
}
